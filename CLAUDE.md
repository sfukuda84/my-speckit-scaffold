# CLAUDE.md

このプロジェクトのエージェント向けルールは `.kiro/steering/` で一元管理している。以下のファイルを読み込み、その内容に必ず従うこと。

@.kiro/steering/language.md
@.kiro/steering/spec-driven-development.md

ルールを追加・変更するときは `.kiro/steering/` のファイルを編集する。このファイル（CLAUDE.md）にルールを直接書かない。新しい steering ファイルを追加したときは、上の読み込みリストにも 1 行追加する。
